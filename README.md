# Aaron Iziyaev & Kubi Nunu

## To run the code

Linux/Mac:

```bash
./venv/bin/activate && pip install -r requirements.txt
```

Windows:

```powershell
venv\Scripts\activate ; pip install -r requirements.txt
```
